# Generated by tools/gen_arduino_p4_sketch.py -- edit SKETCH_YAML there.
# Mirrors README.md step 3 (the Arduino IDE Tools menu) for arduino-cli:
#   arduino-cli compile --profile colopy-p4 .
#   arduino-cli upload  --profile colopy-p4 -p <port> .
# PartitionScheme=huge_app: the 3 MB app slot; the sketch is ~1.03 MB
# (2026-09-03 IDE build), so no custom partition table is needed.
profiles:
  colopy-p4:
    notes: "CrowPanel Advance 7in ESP32-P4; IDE settings per README step 3"
    fqbn: "esp32:esp32:esp32p4:ChipVariant=prev3,PSRAM=enabled,USBMode=default,CDCOnBoot=cdc,PartitionScheme=huge_app,FlashMode=qio,FlashSize=16M,UploadSpeed=921600"
    platforms:
      - platform: "esp32:esp32 (3.3.11)"
        platform_index_url: "https://espressif.github.io/arduino-esp32/package_esp32_index.json"
    libraries:
      - dir: "../../p4/arduino_libraries/ESP32_Display_Panel"
      - dir: "../../p4/arduino_libraries/ESP32_IO_Expander"
      - dir: "../../p4/arduino_libraries/esp-lib-utils"
default_profile: colopy-p4
default_port_config:
  baudrate: 115200
