# MIGRATION-LOG — Drydock refactor (engine-dev-environment-spec.md)

One entry per landed increment: what changed, what was retired, test status.

| # | Increment | Retired | Tests |
|---|---|---|---|
| 0 | Phase 0: GAP-ANALYSIS.md + rulings (C++17; web shell for P1–P2; migration order approved) | — | gate G green (10/10 ctest + selftests + validators) |
| 1 | spec-P0a: canonical text module (`drydock/text/rec_text.{hpp,cpp}` parser + deterministic serializer, canonical int/float, escapes, nesting lint) + `drydock_tests` (21 assertions incl. the byte-identical round-trip) wired as ctest #11 | — | 11/11 ctest green; forge selftests + parity oracle unaffected |
| 2 | spec-P0b: SCHM schema layer — inline dicts + booleans in the text format; `drydock/schema/` (SCHM loader → TypeDef/FieldDef, canonicalizer enforcing schema field order + sorted unordered lists, per-record validator: types/ranges/required/ref-shape/flags); authored `data/schema/{good,unit,prof}.rec` from the @CARGO/@UNIT/@JOB columns (+ EXE-parity `index` ordinal + lossless legacy columns) | — | 11/11 ctest (drydock_tests now 36 assertions, cwd pinned to repo root) |
| 3 | spec-P0c: `data/base/{good,unit,prof}/` — 71 canonical one-record-per-file .rec (generated from the extraction tables by `tools/drydock_migrate.py`, idempotent); `drydockc` CLI (validate + canonical byte-identity + 200-line lint + dangling-ref check + deterministic pack with read-back); `forge/drydock_bridge` — `forge serve` now loads GOOD/UNIT/PROF rules from the canonical text (swap-on-success); new ctest gates: `drydockc_gate` (full-set round-trip), `drydock_migrate_check` (provenance), RuleData parity (poisoned-then-loaded equality vs compiled defaults) | JSON stays authoritative for unmigrated types; nothing retired yet | 13/13 ctest; game boots + runs off the record store for migrated types |
| 4 | spec-P0d: codegen — `drydock-gen` (schema .rec → `dd_gen.hpp`: per-type C++ structs with presence bools, `DDFieldInfo` field-offset reflection tables, `DDTypeInfo` registry, thin load/serialize wrappers) over the generic reflection runtime `core/reflect.hpp`; build-time CMake step (deterministic, no-op when unchanged); reflection test proves record→struct→record is byte-identical. **Spec P0 complete.** | — | 13/13 ctest |
| 5 | spec-P1a: record store (`drydock/core/store.{hpp,cpp}`) — per-type contiguous arrays + id→index hash + u32 handles; bidirectional ref index maintained by every mutation; the `store_set`/`store_add`/`store_delete` chokepoint with schema validation, inverse-op undo journal (set/clear/add/delete all round-trip), safe-delete refusal listing inbound refs, dirty set. 26 new assertions | — | 13/13 ctest |
| 6 | spec-P1b: store-backed HTTP surface (`forge/drydock_api`) — global store loaded at serve start; `/api/dd/{types,records,record,set,add,delete,undo,redo,save,status}`; every mutation through the chokepoint; save re-serializes dirty records to one-per-file canonical text; the live game follows the store (`drydock_apply_records` lockstep, verified: burden edit moves the Europe spread, undo moves it back). Store-set single-field validation refactored out of whole-record validate (probe-record bug). **First strangler cutover:** with the store loaded, GOOD records are the market-knob authority — `live_market_rules` no longer re-reads the legacy @CARGO JSON bindings (Tables-tab @CARGO edits no longer drive the market; the GOOD grid does as of P1c) | @CARGO market-knob authority (JSON bindings) → record store | 13/13 ctest; live-loop probe green |
| 7 | spec-P1c: Object Window + grid view — new **Drydock** tab in the web shell: left type tree with live counts, filter-as-you-type across ids/names/values, sortable columns, inline cell editing through `/api/dd/set`, per-column min/max/mean stats, dirty badges, duplicate-as-new / ref-checked delete, Save/Undo/Redo toolbar. ZERO per-type UI code — the grid is built entirely from the `/api/dd/types` schema. Playwright-verified (edit→filter→undo, no page errors) | — | 13/13 ctest + JS syntax + probe |
| 8 | spec-P1d: TERR (29 ids from @UNFORESTED/@FORESTED×2/@OTHER incl. all 9 yield columns), BLDG (42), CONF (34 rule knobs from cfg.json; list-valued knobs as ordered `values`), PHAS (turn.json pipeline with reads/writes) migrated — 181 records / 7 types; codegen learns skip-with-note for unrepresentable fields + `RType::RefId`; shared `cfg_fields.hpp` X-macro; bridge applies TERR arrays always and CONF scalars at startup only (Rules overlay stays on top until P5); parity extended (TERR arrays + CONF spot-checks poisoned-then-loaded). Authority notes: TERR live via store; PHAS/BLDG passive mirrors under the provenance gate until their consumers cut over | — | 13/13 ctest; live probe (terr edit + undo through the chokepoint) |
| 9 | spec-P2a: form view — click any record id → schema-generated form (number inputs with min/max + range hints, str inputs, ref fields as datalist pickers + jump hyperlinks, per-field doc strings), browser-style back/forward history, used-by inspector from the ref index with clickable sources, duplicate/delete actions, collapsible read-only canonical-text view ('what git sees'). Playwright: navigate → back restores, no page errors | — | 13/13 ctest + JS syntax + probe |
| 10 | spec-P2b: real refs — `prof.produces = ref<good>` on the 18 producing professions, DATA-SOURCED from the engine's profession→good mapping (tile profs 0–8 = the terrain yield columns `y_farmer..y_fisherman`, whose column index IS the good id, `engine.cpp terrain_good_yield`; artisans 9–17 = the `colony_compute_production` outputs incl. the Hammers 16 / Crosses 17 / Liberty Bells 18 pseudo-goods, which already exist as good records at those indices). Non-producing classes (Teacher..Convert) stay absent — lossless. `drydock_migrate.py` carries the mapping + citations so the provenance gate regenerates it; ref pickers, used-by, and safe-delete now run on genuine inbound refs (drydockc dangling-ref check live on 18 refs; delete good.rum → "refused, referenced by prof.distiller.produces"). Fixed form-view datalist fill racing the DOM insert (pickers rendered empty). 9 new `real refs` test assertions over data/base | — | 13/13 ctest + JS syntax + Playwright (picker 20 options, used-by, jump) |
| 11 | spec-P2c: **strangler retirement of the Tables-tab rows for every migrated @SECTION** (@CARGO/@UNIT/@JOB/@BUILDING/@UNFORESTED/@FORESTED/@OTHER — read-only "RETIRED" banner + open-in-Drydock jump; unmigrated sections unchanged). Made honest by a **legacy-reader write-through**: every store mutation (and store init, and any `invalidate_tables()` reload) patches the mapped in-memory JSON cells (`table_patch_cell` in engine.cpp + `drydock_repatch_tables` in drydock_api.cpp), so the un-cut-over `@SECTION[row].col` consumers (terrain yields via `terrain_good_yield`, job/unit display names, ...) follow Drydock edits live — verified: `terr.plains.y_farmer` 4→9 shows through `@UNFORESTED[2].y_farmer` instantly, undo restores; same for `@JOB` names. In-memory only: the .rec files are the persistent truth; the two @FORESTED bands fold onto the same 8 JSON rows (band 2 wins on full repatch — identical values unless edited apart). Rules tab gains the CONF authority note (conf records seed defaults at serve start; the overlay stays the live-tweak layer until P5). Store-only fields (prof.produces) have no JSON column and are skipped | Tables-tab editing for @CARGO, @UNIT, @JOB, @BUILDING, @UNFORESTED, @FORESTED, @OTHER → Drydock grid/form | 13/13 ctest + selftests + validators + live probes (write-through, undo, retired vs editable sections, no page errors) |
| 27 | **EVNT-3c: the audit's confirmed-findings closeout.** The 143-agent fan-out finished: **36/36 independent per-graph re-derivations confirm equivalence** with the extraction (zero discrepancies), and of the 34 code-review findings only 3 survived the 3-lens refutation panel — two were already closed by EVNT-3b's Python duplicate-feed rejection (re-proven here by running the reproduction against the generator: two feeds into one pin → grph fallback), and the last real one is fixed now: **the `dd_graph_save` update path could half-apply** (a hand-crafted name-only HTTP payload committed + journaled the rename via `store_set` before the required-field clear of `nodes` was refused — error returned, mutation kept, record flushed on next dd-save). A structural pre-check now rejects a payload without a proper `nodes`/`edges`/`name` shape BEFORE any mutation; after it, every put on the update path is either decompiler-guaranteed or an optional-field clear, so a rejected save applies NOTHING (proven: same text, same journal depth). The remaining survivor is accepted-and-documented, not fixed: a hand-authored bare-token param normalizes to its `{ ref = ... }` dict twin on first canvas save — the two forms are the SAME JSON through `value_to_json`, so mapping backwards would instead break dict-form byte-stability (dlog widgets); one-time normalization, byte-stable thereafter | — | 12/12 ctest + selftests + validators + migrate --check + the full probe suite incl. HARDEN 5 (rejected update applies nothing) and HARDEN 6 (Python rejects duplicate feeds) |
| 26 | **EVNT-3b: adversarial-audit hardening.** The ultracode fan-out (5 review lenses + 36 per-graph re-derivations, 3-lens refutation) confirmed all 36 records equivalent and surfaced editor-integrity gaps in the machinery, all closed: (a) **the store-brick hole** — a canvas graph whose typed tree nests past the text parser's guard (32) now falls back to lossless grph on save instead of writing an evnt record that would fail to PARSE at next boot (`json_depth` cap in `graph_as_typed`, mirrored in the Python generator); (b) **reclassification is add-before-delete** — a failed validation can no longer destroy the graph (the twin survives), and the record's `notes` prose is carried across the type flip instead of being silently dropped; (c) **`dd_compile_evnt` validates hand-authored records** — dangling `$ghost` args, missing/duplicate var ids, var without `do`, a top list not starting with On*, duplicate/`out` branch pins, an EMPTY branch body (the interpreter's Switch falls through to `default` when a value pin has no edge, so an explicit empty case is unrepresentable), and a branch-bearing step followed by a sibling (the interpreter never follows `out` from Branch/Switch — dead code) are all clear load errors now, not silent misbehavior; (d) **decomposer parity** — the Python generator now enforces the exact C++ constraint set (duplicate exec pins on ANY label, two data feeds into one pin, duplicate node ids, non-dict params, comments beyond `{text:str}`, edges to unknown nodes, float params — whole floats collapse to int like `json_any_to_value`), so a record either generator emits round-trips through the other; (e) **`dd_graph_list` dedups** a grph/evnt twin pair (creatable via the generic add — load already prefers grph deterministically); (f) **engine-wide JSON fixes** — `dump_number` now prints the shortest round-tripping form (plain `%g` truncated at 6 significant digits, corrupting float params across a load/save cycle) and the parser combines UTF-16 surrogate pairs into real 4-byte UTF-8 (was CESU-8); (g) **the canonicalizer enforces sorted dict keys** recursively (previously a generator convention the byte-identity gate could not check). Known-and-accepted: a reclassify is two journal entries (undo ×2 restores; store-level transaction grouping out of scope), ints past 2^53 lose precision in the JSON layer (params are small ints), and a mid-update validation failure can partial-apply (unreachable via decompiler-produced values). Verified: HARDEN probes 1–4 (deep tree→grph, notes both ways, twin listed once, $ghost=error) + the full EVNT-3 suite re-green | — | 12/12 ctest + selftests + validators + migrate --check byte-identical + 36/36 equivalence + 36/36 smoke + 36/36 byte-stable + reclass round trip + 5 HARDEN probes |
| 25 | **EVNT-3: the decomposition is FINISHED — all 36 behavior graphs are typed events (`data/base/grph/` is empty).** The typed form goes fully general: `vars` = the data-producer DAG hoisted out of the exec flow, sorted by id (`{ do = <Type>, id = <original node id>, ...params, args = { pin = "$var" } }` — sharing is by reference, so one impure Roll stays ONE roll however many consumers; producers chain, e.g. combat's atk/def→total→cmp); `steps` = the exec TREE (`branches = [{ pin = <label>, steps = [...] }]` sorted by pin — Branch true/false, Switch cases, popup choice texts; the 'out' continuation is simply the next sibling). Exec JOINS are tail-duplicated (per-path walk with per-path cycle detection — behavior identical, bounded: kings_tax tops out at 19 steps); the type key is `do` because Math/Compare carry a REAL param named `op`, and compiled synthetic ids are '#'-prefixed (`#s<n>`/`#c<n>`) because authored ids like c1 exist. Both decomposers (Python generator + C++ `graph_as_typed` for canvas saves) enforce the same fallback constraints — cycles/multi-trigger/mixed-pin/duplicate-pin shapes stay grph (none of the 36 need it; the type remains as the safety net). Parser nesting guard 4→32 for the deep trees. Equivalence proven three ways: (a) PATH-SET equality — every root→leaf exec path (pin taken, type, params, transitively-resolved feed signatures) identical between the frozen extraction graph and the compiled record, all 36; (b) 36/36 smoke run clean; (c) all 36 no-op canvas saves BYTE-STABLE with a clean journal, plus a live reclassification round trip (second trigger added → grph, removed → evnt byte-identical). Adversarially verified per the ultracode directive: a 41-agent fan-out (5 code-review lenses over decompose/graph_as_typed/dd_compile_evnt/dd_graph_save/canonical-text + 36 independent per-graph re-derivations) with 3-lens refutation of every raised finding | `data/base/grph/*` (all 36 records reclassified to `evnt`; the grph type + lossless path stay as the fallback for future untypable authoring) | 12/12 ctest + selftests + validators + 36/36 path-set equivalence + 36/36 smoke + 36/36 byte-stable no-op saves + reclassification round trip |
| 24 | **EVNT-2: comments + flat data-feeds — 8 more graphs typed (14/36).** Two additive evnt extensions: `comments` = ordered `[{ text, x, y }]` (edge-free Comment annotations, lossless incl. canvas position); per-step `args` = `{ <pin> = { op, ...params } }` — FLAT single-consumer data producers (GetState/Formula/Constant/Roll/... emitting on their `value` pin) feeding step pins like popup %NUMBER fills. The strict rule that keeps it safe: a producer with fan-out is NEVER typed (duplicating a shared impure `Roll` would change behavior) — so `trade_route` (shared Formula) stays grph. Typed now: the 6 goto_*, colony_production/colony_workers/turn_loop (comment chains), scoring/spanish_succession/market_prices/colony_build/demo_treasure (data-fed). Equivalence: all 14 compiled events match the extraction on chain sequence + data feeds + comments; a no-op canvas save of a typed event is BYTE-STABLE with a clean journal (compile→decompile is exact) | — | 12/12 ctest + selftests + validators + 14/14 equivalence + 36/36 smoke + byte-stable round trip |
| 23 | **EVNT-1: the first TYPED events** — pure exec chains (one On* trigger, a single out→in path, no comments/branches/data pins) decompose into `evnt` records: `steps` = the ordered op list, step 0 the trigger (`[{ op = "OnTestFire" }, { op = "Navigate", screen = "europe" }]` IS the legible outline form, spec §8). The 6 goto_* graphs migrate; a deterministic compiler expands steps → graph JSON for the unchanged interpreter; `dd_graph_save` RECLASSIFIES on every canvas save (still a chain → decompile to evnt; gained branches → lossless grph; the stale form is deleted). One-time equivalence proof: each compiled chain's (type, params) exec sequence == the frozen extraction graph, all 6. Also fixed a latent bug: brand-new records from the screen/graph designers now add COMPLETE (store_add validates required fields; add-empty-then-set was refused). Store: 18 types | — | 12/12 ctest + selftests + validators + equivalence proof + 36/36 smoke + live reclassification round trip (Branch added → grph, removed → evnt, steps identical) |
| 22 | **EVNT-0: the 36 node graphs join the store** (`grph` records — the LAST data family). Survey first: 442 nodes across 58 kinds (12 trigger kinds, ~15 logic/gate kinds, ~30 effect kinds; 12 graphs are ≤6-node chains). Ruling followed: a one-step semantic decomposition into typed EVNT/RQMT/EFCT atoms risks drift across 442 nodes, so EVNT-0 stores the graphs LOSSLESSLY (name + ordered `nodes`/`edges` list<dict>, verbatim; int/str values only, dict keys sorted; the atom decomposition now evolves record-side). CRUD cut over through `dd_graph_hooks` (same seam as screens): `list/load/save_graph` — the Logic canvas save is a journaled store edit, and `run_graph`/`FireEvent` execute the store's truth. Generator fix: empty dicts render `{ }` matching the C++ canon. Store: **17 types, 1,230 records**. Verified: the full 36-graph smoke runs clean from records; canvas rename → dirty record → one undo restores | graph authority (graphs/*.json read/write) → record store | 12/12 ctest + selftests + validators + 36/36 graph smoke + journal probe |
| 21 | **P5: the rules overlay is SUBSUMED — the store is the single live rules authority.** CONF applies live on every store mutation (incl. the two list-valued gate-year knobs, now bridged); `drydock_import_overlay` converts a rules-overlay JSON (`cfg` / `units`-by-name / `terrain_defense`/`move`-by-id) into chokepoint `store_set`s (validated, journaled, undoable; no-ops skipped; `defense`→the @UNIT `combat` column). Wired at three seams: **boot** (a persisted active mod imports as record edits), **Rules-tab save** (`/api/rules/save` imports + still writes the exchange file), **reset** (re-inits the store from data/). `move_class`/`capbits` are not record-represented and stay overlay-side (noted in the tab). Verified live: conf edit → `cfg.*` follows instantly + undo; overlay save → "imported (2 edits)", frigate RECORD carries attack 9; reset reverts both; restart re-imports the persisted mod (1 edit) | live rules authority of `mod.cpp`'s overlay (now an import/export exchange format only) | 12/12 ctest + selftests (incl. forge_mod) + validators + live probes (conf live, import, reset, boot re-import) |
| 20 | **AUTHORITY FLIP (ruling Q7)** — `data/base` is the authoring truth from here: it was generated once from the extraction (proven byte-identical through MIGRATION-LOG 3–19) and is now free to diverge as the user edits. The migration-era provenance ctest (`drydock_migrate_check`) RETIRES (the script stays for reference regeneration); the RuleData parity test becomes **loader faithfulness** — poison-then-load now compares against values read independently from the records themselves, not the compiled defaults, so it gates the bridge forever and never a legitimate edit; the one value-pinning reflection assertion (burden == 7) relaxes to presence. Spec fidelity is UNCHANGED: `verify_rules.py` pins the compiled defaults to the byte-verified extraction tables permanently. Proven both ways: all 12 ctest green pristine AND with a simulated user edit (food burden 7→9) in data/base | `drydock_migrate_check` ctest (migration scaffolding) | 12/12 ctest pristine + 12/12 with a diverged data/base |
| 19 | spec-P3c: **SCEN migrated + cutover** — the authored scenario as `scen.new_world` (name/map/calendar/start_gold/default picks, ordered `colonies` + `units` list<dict>; `_doc` carries into `notes`; the one JSON bool, `on_water_adjacent`, migrates as 1 — its consumer is presence-checked). `game_new` seeds from the store record when loaded (file fallback); the bundle exporter iterates scen records. Verified live: new game shows year 1492 / gold 500 / 2 colonies / 16 units from the record; a `start_gold` 500→777 store edit changes the NEXT new game, undo restores — the scenario editor workflow is the Drydock form now. Store: 16 types, 1,191 records | scenario-seed authority (scenarios/*.json) → record store | 13/13 ctest + selftests + validators + live probes (setup-from-record, edit, undo) |
| 18 | spec-P3b: **DLOG migrated + full CRUD cutover** — the 8 screen layouts as `dlog.<id>` records (name/background, ordered `size`, ordered `widgets` list<dict>; widget dict keys stored SORTED so designer saves — which round-trip the C++ sorted-map JSON — are byte-stable against the generator). The screen CRUD cuts over through `dd_screen_hooks` (engine.hpp seam, null in forge_gui): `list_screens`/`load_screen` read the store; **`save_screen` becomes ordinary `store_set` calls** — a Screens-tab designer save is now validated, journaled (no-op field writes skipped, so one rename = one undo), ref-indexed, and lands as canonical text on dd-save; screens/*.json stays the frozen extraction. Store: 15 types, 1,190 records | screen-layout authority (screens/*.json read/write) → record store | 13/13 ctest + selftests + validators + drydockc (1,176 base records) + live probes (list/load/save/one-op-undo) |
| 17 | spec-P3a: **the engine boots the store from the pack** (spec §4.1 release path). `drydockc data -o game.pack` now packs the SCHM records alongside the 1,168 data records — one artifact carries schema + data; `forge serve <port> --pack game.pack` boots the whole store from it (explicit opt-in only, so a stale pack can never silently shadow fresh .rec edits in a dev tree; text loading stays the default). The `drydockc_gate` ctest already regenerates + read-backs `build/game.pack` every run. New `pack boot` test: a text-booted store and a pack-booted store must be BYTE-IDENTICAL record-for-record (1,182 compared). Verified live: serve `--pack` logs "from pack", all 14 types / consumers (messages, labels, sprites, palette, write-through) work identically | — | 13/13 ctest + selftests + validators + live pack-boot probe |
| 16 | spec-P2h: **SPRT + PLTT migrated + consumer CUTOVER** (migration-order item 12 — completes the approved P0–P2 order). `sprt` (437): the sprite catalog verbatim (`sid` keeps the dotted catalog id; label/kind/sheet/frame/file/w/h; the one extraction dup, building.town_hall ×3 tiers, dedups to _2/_3 slugs; no separate atlas artifact exists so sheet+file ARE the ATLS metadata — nothing invented). `pltt.viceroy`: VICEROY.PAL as 256 ordered #rrggbb colors (position = palette index). Cutover: `/api/sprites` (web SPRITECAT) and `/assets/palette.json` (NVPAL — map/minimap/chip colors) rebuild from the store. Store: **14 types, 1182 records**. Verified live: sprite label edit follows through /api/sprites, undo restores; palette serves 256 colors (index 12 = #ff0000, the England chip red) | sprite-catalog + palette authority (sprites.json / palette.json) → record store | 13/13 ctest + selftests + validators + drydockc (14 types, 1168 base records, 0 errors) + live probes + Play tab loads with no page errors |
| 15 | spec-P2g: **TEXT migrated + consumer CUTOVER** (migration-order item 11, label half) — 21 verbatim line-list sections as `text.<source>_<section>` records (LABELS ×7 incl. @MISC whose line indices the specs cite, COLONY ×5 name pools, MENU ×8 pulldowns, NAMES @SEASONS; the NAMES CSV tables stay typed records — never duplicated as text). `labels_section` (every screen string) reads the store first when loaded; `/api/text` overlays migrated sections; `/api/labels` follows. `json_to_value` learns list<int>/list<str> so ordered lists are API-editable (also unlocks conf.values / phas.reads-writes edits). Store: 12 types, 741 records. Verified live: @MISC[46] "OK" → list edit → both /api/labels and /api/text serve "Okay!", undo restores | @SECTION label/menu/name-pool text authority (_sections.json) → record store | 13/13 ctest + selftests + validators + drydockc (730 base records, 0 errors) + live probes |
| 14 | spec-P2f: **MSGE migrated + consumer CUTOVER** (migration-order item 11, message half) — 499 GAME.TXT message records (`msge.<slug>`: verbatim `key` + `text` with %NUMBER/%STRING placeholders, box_w/x/y, sprite/speaker/default channels; "none" defaults stay absent, lossless; one record per file — §4.2's ~50/file grouping deferred so the save/byte-identity machinery stays uniform). The @KEY readers are keyed lookups, so they CUT OVER instead of write-through: `dd_message_hook` (engine.hpp seam, installed at store init; null in forge_gui builds) serves `message_meta` (ShowPopup box/sprite) and `game_message_text` (tutorial/notice fills) from the store; `/api/messages` (the web MSGREC) rebuilt from it. Codegen learns to sanitize C++-keyword member names (`default` → `default_`, reflection keeps the schema name); form view renders multi-line strings in a textarea (a plain input silently DROPPED the \n). Store: 11 types, 720 records. Verified live: edit `msge.dos` text → /api/messages serves the new verbatim text, undo restores | @KEY message authority (messages.json) → record store | 13/13 ctest + selftests + validators + live probes (store-served /api/messages, edit+undo, textarea keeps newlines, no page errors) |
| 13 | spec-P2e: **NATN + FFAT migrated** (migration-order items 9–10) — `natn` (4 records) merges the row-parallel @COUNTRY (name, color 9–15 chip palette) and @NATIONALITY (nationality) tables on the load-bearing power-slot ordinal; `ffat` (25 records) carries @FATHERS verbatim (name, category id per @FOUNDING, three era-band offer weights, founding_fathers.md §3). `drydock_migrate.py` generates both under the provenance gate; the write-through fans one natn record out to BOTH legacy tables (verified live: color 12→13 shows through `@COUNTRY[0].color`, nationality through `@NATIONALITY[0].name`, ffat weight through `@FATHERS[0]`, undo restores all); Tables-tab rows for @COUNTRY/@NATIONALITY/@FATHERS retired (read-only + jump). Store: 10 types, 220 records | Tables-tab editing for @COUNTRY, @NATIONALITY, @FATHERS → Drydock | 13/13 ctest + selftests + validators + live probes |
| 12 | spec-P2d: **SCHM in the store — the registry describes itself** (migration-order item 8). New `data/schema/schm.rec` meta-schema (`version` int + ordered `fields` list<dict>, documenting the field-dict keys and that schema edits take effect at rebuild when drydock-gen regenerates); `drydock_store_init` loads the 8 schm.* records into the store — the Object Window now browses `schm` like any type, `schm.schm` is the fixed point; `dd_save` routes dirty schm records to `data/schema/<code>.rec` (schm records ARE the schema files). Form view renders flags/list/dict kinds as read-only structured views instead of corruptible text inputs (also fixes phas reads/writes + conf values rendering). drydockc/codegen now see 8 types (DDSchm struct: version + skip-note for the list field) | — | 13/13 ctest + selftests + validators + Playwright (schm in tree, schm.prof form, read-only structured fields, no page errors) |
